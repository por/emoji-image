{
  "name": "react-native-apple-emoji",
  "version": "0.1.0",
  "description": "Use Apple-style emoji’s cross-platform by falling back on static images.",
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "npm run build"
  },
  "author": "Tim Bormans",
  "license": "MIT",
  "devDependencies": {
    "fontkit": "^1.7.7",
    "mkdirp": "^0.5.1",
    "rimraf": "^2.6.2"
  },
  "files": [
    "dist"
  ]
}
